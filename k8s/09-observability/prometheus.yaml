apiVersion: apps/v1
kind: Deployment
metadata:
  name: design91-prometheus
  namespace: design91-infrastructure
  labels:
    app: design91-prometheus
    component: metrics
    project: design91
spec:
  replicas: 1
  strategy:
    type: Recreate  # Prevents concurrent pods accessing same PVC
  selector:
    matchLabels:
      app: design91-prometheus
  template:
    metadata:
      labels:
        app: design91-prometheus
        component: metrics
        project: design91
    spec:
      terminationGracePeriodSeconds: 60  # Allow graceful Prometheus shutdown
      containers:
      - name: prometheus
        image: prom/prometheus:v2.47.0
        ports:
        - containerPort: 9090
          name: http
        args:
        - '--config.file=/etc/prometheus/prometheus.yml'
        - '--storage.tsdb.path=/prometheus'
        - '--web.console.libraries=/etc/prometheus/console_libraries'
        - '--web.console.templates=/etc/prometheus/consoles'
        - '--storage.tsdb.retention.time=200h'
        - '--web.enable-lifecycle'
        volumeMounts:
        - name: prometheus-config
          mountPath: /etc/prometheus/prometheus.yml
          subPath: prometheus.yml
        - name: prometheus-data
          mountPath: /prometheus
        livenessProbe:
          httpGet:
            path: /-/healthy
            port: 9090
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /-/ready
            port: 9090
          initialDelaySeconds: 10
          periodSeconds: 10
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "kill -TERM 1"]
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      volumes:
      - name: prometheus-config
        configMap:
          name: design91-prometheus-config
      - name: prometheus-data
        persistentVolumeClaim:
          claimName: design91-prometheus-data-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: design91-prometheus
  namespace: design91-infrastructure
  labels:
    app: design91-prometheus
    component: metrics
    project: design91
    port-forward: "enabled"
  annotations:
    port-forward.design91.io/ports: "9090"
    port-forward.design91.io/description: "Prometheus metrics and monitoring"
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    name: http
  selector:
    app: design91-prometheus
