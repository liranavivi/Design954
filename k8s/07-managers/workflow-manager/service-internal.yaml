apiVersion: v1
kind: Service
metadata:
  name: design91-workflow-manager
  namespace: design91-infrastructure
  labels:
    app: design91-workflow-manager
    component: manager
    project: design91
    manager-type: workflow
    service-type: internal
    port-forward: "enabled"
  annotations:
    port-forward.design91.io/ports: "5180"
    port-forward.design91.io/description: "Workflow Manager API access"
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
spec:
  type: ClusterIP
  ports:
  - port: 5180
    targetPort: 5180
    protocol: TCP
    name: http
  selector:
    app: design91-workflow-manager
  sessionAffinity: None
  
---
apiVersion: v1
kind: Endpoints
metadata:
  name: design91-workflow-manager
  namespace: design91-infrastructure
  labels:
    app: design91-workflow-manager
    component: manager
    project: design91
# Endpoints will be automatically populated by Kubernetes based on pod readiness
